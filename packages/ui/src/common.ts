import { SG, SY, SLR10T } from '@busmap/components/colors'

export const PredictedVehiclesColors = {
  green: SG,
  yellow: SY,
  red: SLR10T
}
export const STORAGE_KEYS = {
  themeMode: 'busmap-themeMode',
  vehicleSpeedUnit: 'busmap-vehicleSpeedUnit',
  vehicleColorPredicted: 'busmap-vehicleColorPredicted',
  predictionsFormat: 'busmap-predsFormat'
}
